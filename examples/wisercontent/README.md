# WiserContent Example

This folder is a frozen example of generated output from the PRD framework.

## Features
- Manual regression fixture for the formal PRD workflow
- Snapshot reference for generated project structure

## Quick Start
- Inspect `docs/wisercontent-prd-v2.md` first
- Review companion docs under `docs/`

## Project Structure
- Root files mirror a generated project package
- `docs/` contains the generated reference documents

## Development
- Regenerate manually when structural template changes require a new snapshot
- Do not auto-sync this example from framework source files

## Snapshot Policy
- It is a manual regression fixture.
- It is regenerated only when template or workflow changes require a new reference snapshot.
- It is not a live baseline and must not auto-sync with framework source files.
